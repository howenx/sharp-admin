<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.SubjectPrice">
    <sql id="subjectPriceColumns">
        ${alias}.id,
        ${alias}.theme_id,
        ${alias}.inv_id,
        ${alias}.price,
        ${alias}.discount,
        ${alias}.create_at,
        ${alias}.update_at
    </sql>
    <insert id="sbjPriceAdd">
        insert into subject_price(theme_id,inv_id,)



    </insert>

</mapper>