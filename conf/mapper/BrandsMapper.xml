<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.BrandsMapper">

    <!--enable mybatis default cache configure reference:
        https://mybatis.github.io/mybatis-3/zh/sqlmap-xml.html#cache
    -->
    <sql id="brandsColumns">
        ${alias}.brand_id,
        ${alias}.desc_cn,
        ${alias}.desc_en,
        ${alias}.desc_kr,
        ${alias}.logo_url,
        ${alias}.home_page,
        ${alias}.create_date,
        ${alias}.brand_nm_cn,
        ${alias}.brand_nm_en,
        ${alias}.brand_nm_kr
    </sql>

    <select id="getBrands" resultType="entity.Brands" parameterType="java.lang.Long">
        select
        <include refid="brandsColumns">
            <property name="alias" value="t"/>
        </include>
        from brands t where brand_id=${value}
    </select>

    <select id="getAllBrands" resultType="entity.Brands">
        select
        <include refid="brandsColumns">
            <property name="alias" value="t"/>
        </include>
        from brands t where 1=1
    </select>
</mapper>
