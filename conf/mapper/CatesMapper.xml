<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CatesMapper">


    <!--enable mybatis default cache configure reference:
        https://mybatis.github.io/mybatis-3/zh/sqlmap-xml.html#cache
    -->

    <sql id="catesColumns">
        ${alias}.cate_id,
        ${alias}.pcate_id,
        ${alias}.cate_nm,
        ${alias}.cate_desc
    </sql>

     <select id="getCate" resultType="entity.Cates" parameterType="java.lang.Long">
        select
        <include refid="catesColumns">
             <property name="alias" value="t"/>
        </include>
             from cates t where cate_id = #{cateId}
     </select>

    <select id="getParentCates" resultType="entity.Cates">
        select
        <include refid="catesColumns">
            <property name="alias" value="t"/>
        </include>
        from cates t where t.pcate_id IS NULL ORDER BY t.cate_nm;
    </select>

    <select id="getSubCates" resultType="entity.Cates" parameterType="map">
        select
        <include refid="catesColumns">
            <property name="alias" value="t"/>
        </include>
        from cates t where 1=1
        <if test="parentCateId !=null">
            AND pcate_id = #{parentCateId}
        </if>
        <if test="cateId !=null">
            AND cate_id = #{cateId}
        </if>
    </select>

    <insert id="insertCates"  parameterType="entity.Cates" useGeneratedKeys="true" keyProperty="cate_id">
        insert into cates
        (pcate_id,cate_nm,cate_desc,cate_code)
        values(#{pcateId},#{cateNm},#{cateDesc},#{cateCode})
    </insert>
</mapper>
